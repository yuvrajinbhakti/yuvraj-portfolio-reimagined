# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets
# `*.local` above only catches .env.local — plain `.env` was committable, and
# ENV_SETUP_GUIDE.md instructs creating exactly that with the EmailJS keys.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent tooling (machine-local permissions, generated context)
.claude/
AGENTS.md
.gstack/

# Local state for `wrangler dev` — a simulated Durable Object's SQLite file.
# Machine-specific, and it reappears every time the relay is run locally.
presence/.wrangler
.vercel
